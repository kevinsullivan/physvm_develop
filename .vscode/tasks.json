{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "cd src && make -j 4 && make install",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "group": { 
        "kind": "build",
        "isDefault": true
    }
}