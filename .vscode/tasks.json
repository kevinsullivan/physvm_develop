{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "cd /peirce/PeirceGen && mcs -out:PeirceGen.exe *.cs Generators/*.cs && mono PeirceGen.exe && cp -r /peirce/PeirceGen/symlinkme/* /peirce/src && cd /peirce/src && make && make install",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "group": { 
        "kind": "build",
        "isDefault": true
    }
}
